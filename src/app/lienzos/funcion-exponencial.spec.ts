import { FuncionExponencial } from './funcion-exponencial';

describe('FuncionExponencial', () => {
  it('should create an instance', () => {
    expect(new FuncionExponencial()).toBeTruthy();
  });
});
