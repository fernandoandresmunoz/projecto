import { NextGenGliderStrategy } from './next-gen-glider-strategy';

describe('NextGenGliderStrategy', () => {
  it('should create an instance', () => {
    expect(new NextGenGliderStrategy()).toBeTruthy();
  });
});
