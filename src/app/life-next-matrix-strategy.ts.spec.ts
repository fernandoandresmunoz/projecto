import { LifeNextMatrixStrategy.Ts } from './life-next-matrix-strategy.ts';

describe('LifeNextMatrixStrategy.Ts', () => {
  it('should create an instance', () => {
    expect(new LifeNextMatrixStrategy.Ts()).toBeTruthy();
  });
});
